export interface AllCoin {
  id: number;
  name: string;
  symbol: string;
  stellar_issuer: string;
  bsc_address: string;
  decimal: number;
  type: number;
  is_active: number;
  created_at?: string;
  updated_at?: string;
}

// export interface Balances {

// }
